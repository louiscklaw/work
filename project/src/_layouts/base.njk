<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>

    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/> {# font #}

    {% include "partials/_google_web_fonts.njk" %}

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/simple-scrollbar@latest/simple-scrollbar.css"/>

    <script src="https://cdn.jsdelivr.net/npm/simple-scrollbar@latest/simple-scrollbar.min.js"></script>

    <link rel="stylesheet" href="{{ '/assets/css/tailwind.css' | transformWithHtmlBase }}" >

    {# Meta #}

    <title>{{ site.name }}
      {% if title %}
        - {{ title }}
      {% endif %}
    </title>

    <meta
      name="description"
      content="
        {% if description %}
          {{ description }}
        {% else %}
          {{ site.description }}
        {% endif %}
        "/>

    {% include "partials/_google_ga.njk" %}

    <style>
      {% include "./style.njk" %}
    </style>

    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>


  </head>

  <body
    class="text-gray-700 text-md leading-10 text-color"
    style="
      overflow-x: hidden;
      font-family: Noto Sans TC, sans-serif; letter-spacing: 0.2rem;
      width: 100vw;
      "
    >
    {% include "partials/_navigation.njk" %}

    <main class="w-screen">
      {% block content %}
        {{ content | safe }}
      {% endblock %}
    </main>

    {% include "partials/_google-event-track.njk" %}

    {% include "partials/_footer.njk" %}
  </body>
</html>
