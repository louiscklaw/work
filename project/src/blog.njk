---
layout: base
title: 開發日誌
description: This is the blog home
tags: page
order: 3
---
{# 
    <h1 class="text-4xl font-bold mb-8">Blog Home</h1>

    <ul class='space-y-4'>
        {% for post in collections.post %}
            <li class='shadow-lg p-4 ' >
                <a class="text-lg block hover:text-sky-400 font-semibold text-gray-700" href="{{ post.url }}">{{ post.data.title }}</a>
                <small class="text-sm text-gray-500">{{post.date | postDate}}</small>
            </li>
        {% endfor %}
    </ul> 
#}

<div style="height: calc( 100vh - 4rem )">
    <div class="flex justify-center items-center h-full">
        <div class="grid grid-cols-1 gap-12">
            <div 
                class="
                    w-[50vw] h-[50vw] 
                    md:w-[400px] md:h-[300px] 
                    rounded 
                    bg-contain bg-center bg-no-repeat
                    "
                    style="
                    background-image: url( {{ '/assets/images/construction.png' | htmlBaseUrl  }} )
                    "
            >
            </div>
            <h2 class="text-2xl text-center leading-10" style="letter-spacing: 0.2rem;"><span class="text-3xl primary-color font-bold">喱</span>頁做緊啊，<br class="inline md:hidden"/>遲D出世... 😂</h2>
        </div>
    </div>
</div>